package vn.edu.fpt.transitlink.monitoring.service;

import vn.edu.fpt.transitlink.monitoring.dto.MonitoringDTO;

public interface MonitoringService {

    MonitoringDTO viewMonitoring(MonitoringDTO monitoringDTO);
}
